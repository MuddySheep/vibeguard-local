Part B — The MAJOR/BLOCKER list from the OSS feature pass
Pulling this back from earlier in the conversation, filtered to OSS-only (skipping anything that was Cloud-territory). These are not the same as the boundary fixes — these are features you haven't built yet that would make the OSS materially stronger.
The five I rated BLOCKER for OSS
#ItemStatusNotes1--fix autofix mode in CLI✅ shipped this rounddone2Web playground (GitHub Pages)✅ shipped this rounddone, tested, working3ESLint plugin⏳ not yetWhere indie devs actually live (the editor)4"First-run" npx ... init experience⏳ not yetSmart version with npm run lint:sql script5Three new catches (SQL-013/014/015)✅ shipped this rounddone, all caught in adversarial testing
3 of 5 BLOCKERs done in 48 hours. That's why I called the iteration fast.
The two outstanding BLOCKERs:
#3 ESLint plugin — eslint-plugin-vibeguard linting tagged template literals (sql`...`) in TypeScript files at write-time. Default tag list ['sql'] (covers postgres.js, Kysely), configurable. ESLint 9 / flat config first; share the autofix code with the CLI via packages/sdk. Drizzle deferred to v2. This is the single biggest remaining piece of the indie-dev story because most indie devs never run a CLI — they live in their editor.
#4 npx vibeguard-local init — Smart version: detects package.json, adds npm run lint:sql script, scaffolds vibeguard.example.sql with a deliberate catch, runs analyze, prints colored output. The npm run lint:sql script is what makes adoption stick — every future npm test is a touchpoint.
The MAJORs (next tier — compound into momentum, not launch-blockers)
#ItemStatus6Three more AI-specific catches (SQL-013/014/015)✅ shipped — same as BLOCKER #57Schema-aware mode (opt-in, no row counts)⏳ not yet — leave for v1.28Drizzle and Prisma adapters as sub-packages⏳ not yet — @vibeguard-dev/drizzle, @vibeguard-dev/prisma9GitHub Action (vibeguard-dev/scan-action@v1)⏳ not yet — PR-comment bot10Per-catch suppression with comments⏳ not yet — -- vibeguard-disable-next-line SQL-00611"Rules you can write yourself" guide + starter template⏳ not yet — turns power users into evangelists