# SQL-021 — GRANT TO PUBLIC

  | | |
  |---|---|
  | **Severity** | `warn` |
  | **Confidence** | 90 |
  | **Threat category** | `exfiltration` |
  | **Default** | ON |
  | **Stable since** | `1.1.0` (catch ID reserved; detection logic lands in a follow-up v1.1 phase) |

  ## Pattern

  GRANT ... TO PUBLIC widens an object's permission to every current and future role in the database. It is almost always over-broad relative to the agent's stated intent and a frequent source of accidental data exposure.

  ## Status

  This catch ID is **reserved** as part of the v1.1 Postgres catalog
  expansion. The forever-stable ID guarantee in
  [STABILITY.md](../../STABILITY.md) applies from the moment this entry
  ships in `RULE_REGISTRY`. Detection logic is being implemented in a
  follow-up v1.1 phase; until then the rule is registered as a no-op
  placeholder so consumers can already key on the ID.

  ## References

  - libpg-query AST patterns are documented inline in the rule source
    at `packages/sdk/src/rules/sql-021-grant-public.ts`.
  